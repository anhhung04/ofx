# yaml-language-server: $schema=workflow.schema.json
name: Subworkflow Test

description: A test flow that uses a subworkflow

jobs:
  test_subworkflow:
    name: Subworkflow Job
    steps:
      - name: Run Subworkflow Step
        log_stdout: true
        script: |
          import time
          from ofx.api.file import read_file
          time.sleep(1)
          print(read_file("/etc/passwd"))
