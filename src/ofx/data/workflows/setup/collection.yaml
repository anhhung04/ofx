name: setup
version: 1.0.0
description: 'Environment and infrastructure setup workflows — cloud VPS provisioning,
  Docker security labs, proxy/VPN/tunnel configuration, Git pre-commit secret scanning,
  and security lab environments.

  '
author: OFX Community
tags:
- setup
- infrastructure
- cloud
- environment
dependencies: []
workflows:
- cloud-setup.yml
- docker-lab.yml
- git-secrets-guard.yml
- proxy-config.yml
