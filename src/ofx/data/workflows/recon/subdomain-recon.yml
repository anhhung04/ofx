# yaml-language-server: $schema=~/.ofx/workflow_schema.json
name: subdomain-recon
description: >
  Subdomain reconnaissance — discovers subdomains using passive and active techniques,
  verifies with DNS probes, checks for takeover vulnerabilities, and hunts for secrets.
tags: [recon, dns, http, ssl, secrets, takeovers]

dispatch:
  inputs:
    target:
      required: true
      type: string
      description: Target domain (e.g. example.com)

jobs:
  passive-enum:
    name: "Passive Subdomain Enumeration"
    steps:
      - task: subfinder
        name: subfinder-enum
        with:
          target: "{{ inputs.target }}"
          all: true

      - task: gau
        name: gau-urls
        with:
          target: "{{ inputs.target }}"
          subs: true

      - name: collect-subdomains
        script: |
          import json

          all_subs = set()

          sf_outputs = {{ steps[0].outputs.typed_outputs | tojson }}
          for item in sf_outputs:
              if item.get('_type') == 'subdomain':
                  all_subs.add(item['host'])

          gau_outputs = {{ steps[1].outputs.typed_outputs | tojson }}
          for item in gau_outputs:
              if item.get('_type') == 'url':
                  host = item.get('host', '')
                  if host and '{{ inputs.target }}' in host:
                      all_subs.add(host)

          with open('/tmp/subdomains.txt', 'w') as f:
              f.write('\n'.join(sorted(all_subs)))

          print(f"Found {len(all_subs)} unique subdomains")
          with open('{{ env.OFX_OUTPUTS }}', 'a') as f:
              f.write(f"subdomain_list={chr(10).join(sorted(all_subs))}\n")
              f.write(f"subdomain_count={len(all_subs)}\n")

  dns-verification:
    name: "DNS Verification"
    needs: [passive-enum]
    steps:
      - task: dnsx
        name: verify-dns
        with:
          target: "{{ jobs['passive-enum'].outputs.subdomain_list }}"
          a: true
          cname: true
          resp: true

  http-probe:
    name: "HTTP Probing"
    needs: [dns-verification]
    steps:
      - task: httpx
        name: probe-subdomains
        with:
          target: "{{ jobs['passive-enum'].outputs.subdomain_list }}"
          tech_detect: true
          status_code: true
          title: true
          follow_redirects: true

      - name: export-urls
        run: |
          echo "live_urls={{ urls(steps[0].outputs.typed_outputs) | selectattr('status_code', 'gt', 0) | map(attribute='url') | unique | join('\n') }}" >> $OFX_OUTPUTS

  takeover-check:
    name: "Subdomain Takeover & Vulnerability Check"
    needs: [http-probe]
    steps:
      - task: nuclei
        name: takeover-scan
        with:
          target: "{{ jobs['passive-enum'].outputs.subdomain_list }}"
          tags: "takeover"

      - name: summary
        run: |
          echo "======================================="
          echo " Subdomain Recon: {{ inputs.target }}"
          echo "======================================="
          echo "Subdomains found: {{ jobs['passive-enum'].outputs.subdomain_count }}"
          echo "Takeover vulns: {{ vulns(steps[0].outputs.typed_outputs) | length }}"
